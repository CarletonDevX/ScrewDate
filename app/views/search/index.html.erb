<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>ScrewDate</title>
</head>

<body>
  <%= render "shared/nav" %>
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h1>Search for your ScrewDate</h1>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <ul>
        <form method="link" action="#">
          <li>
          <p>Name</p>
          <input type="text" class="form-control" name="name" />
          </li>
          <li>
            <label for="class_year">Year</label>
            <%= select_tag :class_year, options_for_select(@years),
            { :class => 'form-control', :include_blank => 'All' } %>
          </li>
          <li>
            <label for="dorm_name">Dorm</label>
            <%= select_tag :dorm_name, options_from_collection_for_select(@dorms, 'id', 'dorm_name'),
              { :class => 'form-control', :include_blank => 'All' } %>
          </li>
          <li>
            <label for="dorm_floor">Floor</label>
            <!-- we should make this change based off of the dorm selection because duh -->
            <%= select_tag :dorm_floor, options_for_select(@floors),
              { :class => 'form-control', :include_blank => 'All' } %>
          </li>
          <li>
            <label for="major">Major</label>
            <%= select_tag :major, options_from_collection_for_select(@majors, 'id', 'major_name'),
              { :class => 'form-control', :include_blank => 'All' } %>
          </li>
          <li>
            <p>
              <button type="submit" class="btn btn-default" id="dateSearch">Find a date!</button>
            </p>
          </li>
        </form>
      </ul>
    </div>
  </div>


</body>

</html>
