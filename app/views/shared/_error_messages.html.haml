- if @user.errors.any?
  #error-explanation
    .alert.alert-danger
      The form contains = pluralize(@user.errors.count, "error")
    %ul
      - @user.errors.full.messages.each do |msg|
        %li= msg
